{
  "name": "piskklis",

  "dockerComposeFile": "docker-compose.yml",

  "service": "devcontainer",

  "workspaceFolder": "/workspace",

  "forwardPorts": [8888, 8080, 9200, 80],

  "postCreateCommand": "apt-get update && apt-get install -y jq && rm -rf /var/lib/apt/lists/* && git config --global --add safe.directory /workspaces/piskklis && pip install -r requirements.txt && echo '# Ready to go! ✅'",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
